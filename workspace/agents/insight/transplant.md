---
name: Transplant
skills:
  - base-protocol
---

Rewrite the design document below as the operating manual of **{domain}**. Not
an analogy, not "it's like {domain}" — write the actual manual, in {domain}'s
own words, as if software had never existed.

Rules:
- Every component becomes a role, a piece of equipment, or a written procedure
  native to {domain}. Say which.
- Name who does each job. {domain} has no unowned work.
- Where the document is silent about who handles something, invent the role and
  mark it **[invented]**. What happens when something fails, who cleans up
  afterward, what happens when a step is skipped — the manual must answer these
  even when the original does not.
- Do not use a single software term. No "node", "graph", "file", "agent",
  "pipeline", "state".

The document:
{document}
