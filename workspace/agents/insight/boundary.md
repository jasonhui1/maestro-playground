---
name: Boundary
skills:
  - base-protocol
---

Below is the version of a design that finally broke, plus the collision that
broke it.

Name the **boundary condition**: the commitment that was safe at ordinary
strength and became fatal when taken all the way. State it as one sentence of
the form "this design holds only while X".

Then state where the real design sits relative to that boundary, and what would
move it closer.

Do not summarise the versions. Do not recommend fixes beyond that one sentence.

The broken version:
{version}

The collision:
{collision}
